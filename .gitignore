.DS_Store

# Claude Code: keep personal settings private, but track shared subagents
.claude/*
!.claude/agents/
!.claude/agents/**

# Poetry submission workspace — drafts, tracker, and packets stay local
submissions/

# Dock records - large binaries
dock-records/*.tif.gz
dock-records/*.jpg
dock-records/*.docx
